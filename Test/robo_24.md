# 23-24 春学期《人工智能与机器学习》回忆卷（机器人工程班）

> 综合两位同学的回忆整理；不完整处以 *[补全]* 标注。

## 一、选择题（2 分 × 20；默认单选，仅 1 题为多选）

1. 通过内部状态（类似"世界模型"）、遵循条件-行动规则的 Agent 类型是：
   - A. 简单反射式  B. 基于目标  C. **基于模型的简单反射式**  D. 基于效用

2. **医疗影像**所对应的任务环境属于下列哪种？
   - A. 可观察的  B. *[补全]*  C. 静态的  D. 分段的（episodic）

3. 哪个算法用到了**玻尔兹曼退火**（Boltzmann 分布）？ *[补全：模拟退火]*

4. **Find-S** 算法输出的假设是：
   - A. 一般的  B. **特殊的**  C. 简单的  D. 复杂的

5. **Find-S** 在 $H$ 中选择与（____）一致的、最（____）的假设。
   - 第一个空：正例/反例；第二个空：一般/特殊（答案：**正例 / 特殊**）

6. **候选消除**算法输出（）可行的假设。
   - A. **全部**  B. 部分  C. *[补全]*  D. *[补全]*

7. **奥卡姆剃刀**倾向于选择（）的假设：
   - 答案：最**简单**的

8. 将命题"任意时间都存在某些的人"翻译为逻辑语句。 *[补全：$\forall t\, \exists x\, \mathrm{Person}(x,t)$]*

9. 关于 $\alpha$-$\beta$ 剪枝描述正确的是：
   - A. 需要遍历决策树
   - B. $\alpha$ 是 MAX 节点的最大值
   - C. $\beta$ 是 MAX 节点的最小值
   - D. 只剪叶子节点

10. 下列哪种算法属于**积极学习**（eager learning）？
    - A. 径向基函数 (RBF)  B. 局部加权回归  C. KNN  D. 基于案例

11. 下列哪种属于**盲目搜索**？
    - A. *[补全]*  B. 贪婪最好优先  C. 一致代价  D. *[补全]*

12. 下列哪一项**不是**遗传算法的步骤？
    - 候选：选择 / 交叉 / **析取** / 变异

13. 关于**迭代加深**（IDA）算法说法**错误**的是？ *[补全]*
    - 干扰项涉及"空间复杂度 $O(bm)$"

14. （多选）下列语句判断哪些正确？（考察合取/析取、全称/存在量词的对偶性）*[补全]*

15. **感知器**的输出取值为 1 或（____）。
    - A. 1  B. 2  C. 3  D. **0**（实际常为 $\{0,1\}$ 或 $\{-1,1\}$，此处按 PPT 口径）

16. **sigmoid** 是一个光滑（）的函数。
    - A. 可微  B. **无限可微（光滑）**  C. 二次可微  D. 不可微

17. 使损失（）符合**极大似然估计**。
    - A. **最小平方和**  B. 最小和  C. *[补全]*  D. *[补全]*

18. **Gibbs 算法**分类错误率至多是贝叶斯最优分类器的（____）倍。 *[答案：2 倍]*

19. *[待补充]*

20. *[待补充]*

## 二、简答题（5 分 × 4）

1. 给定包含 **24 个叶子节点**的博弈树，用 $\alpha$-$\beta$ 剪枝求解并给出博弈路径。
2. 由启发式函数**一致性**（$h(n) \le C_{n,n'} + h(n')$，其中 $n'$ 是 $n$ 的后继，$C$ 是实际边代价）证明其**可容许性**（admissibility）。
3. **过拟合**的定义是什么？
4. **Find-S** 算法的不足是什么？

## 三、大题（10 分 × 4）

1. 给定搜索图，求**一致代价搜索**的展开过程。OPEN 表使用了什么数据结构？路径耗散函数要满足什么条件才能保证最优性？
2. 给定谓词逻辑语句：
   1. 转化为**合取范式（CNF）**；
   2. 用归结反证法证明给定结论，指出**合一（unification）**过程。
3. **扫地机器人 MDP 建模**（PPT 原题）：$5\times5$ 方格地图，用 $0\text{-}24$ 编号；动作成功概率 0.80，不动 0.15，反向 0.05。给出 MDP 四要素。
4. 布尔学习任务中，100 个样例有 80 个被正确分类。求 $\mathrm{error}_h(D)$ 的**标准差**与 **90% 置信区间**（$z_{90\%}=1.64$）。
