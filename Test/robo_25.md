# 24-25 春学期《人工智能与机器学习》回忆卷（机器人工程班）

> 回忆卷，补全处以 *[补全]* 标注。

## 一、选择题（2 分 × 20）

1. "智能体追踪记录世界的状态、要达到的目标，并选择导致达成目标的行动"——这是什么类型的智能体？
   - **基于目标的智能体**

2. **计时棋博弈**的任务环境**不是**下列哪一种？
   - 完全可观 / 静态 / 确定 / 片段
   - *（提示：计时棋是动态的，所以"静态"错；完整选项视原卷而定）*

3. 关于**迭代最佳优先搜索**不正确的是：
   - A. 空间复杂度是 $O(bd)$
   - B. 满足可容纳性则算法是最优的
   - C. 对空间利用率高
   - D. *[补全]*

4. **爬山算法**解决"初始点敏感"问题的改进算法是：
   - A. 随机采用多个初始点（随机重启）
   - B. 模拟退火算法
   - C. 改步长
   - D. 以一定概率选择

5. 下列子句中哪个是 **Horn 子句**？
   - A. $p(a) \lor p(b) \lor \lnot p(c)$
   - B. $\lnot p(a) \lor \lnot p(b) \lor \lnot p(c)$
   - C. $\lnot p(a) \lor p(b) \lor \lnot p(c)$
   - D. $\lnot p(a) \lor \lnot p(b) \lor p(c)$ *(Horn：至多一个正文字)*

6. **Find-S** 算法选择的是（）中最（）：
   - A. 正例，特殊  B. 负例，特殊  C. 正例，一般  D. 负例，一般

7. 下面哪个**不是**启发式搜索算法？
   - A. A\*  B. 贪心  C. 迭代最优性算法  D. **一致代价搜索**

8. **感知器**的输出是 $1$ 和（）：
   - A. 0  B. $-1$  C. 5  D. 4

9. 关于 **sigmoid** 函数说法正确的有哪些（多选）？
   - A. 值域是 $(0,1)$
   - B. 导数是 $y(1-y)$
   - C. 可以直接应用于多分类任务 *(错，多分类用 softmax)*
   - D. 与交叉熵结合使用可以解决梯度爆炸问题 *[补全判断]*

10. 关于**中心极限定理**说法正确的是？ *[补全]*

11. 关于 **Gibbs 算法**正确的有哪些（多选）？ *[补全：Gibbs 的期望错误率不超过贝叶斯最优的 2 倍]*

12. 关于**候选消除算法**错误的有哪些（多选）？
    - A. 只处理正例  B. 只处理反例  C. *[补全]*  D. *[补全]*

13. 关于**样本错误率**与**真实错误率**的说法正确有哪些（多选）？
    - A. 当过拟合时样本错误率显著小于真实错误率

14. **ID3 决策树**学习算法中，选择节点划分属性主要依据什么？
    - A. **信息增益**  B. *[补全]*  C. 基尼系数  D. *[补全]*

15. 将"任何人都有人喜欢"转化为一阶逻辑语句。 *[补全：$\forall x\,\mathrm{Person}(x) \to \exists y\, \mathrm{Like}(y,x)$]*

16. 关于 $\alpha$-$\beta$ 剪枝说法正确的是：
    - A. $\alpha$ 是 MAX 节点的最优值
    - B. $\beta$ 是 MAX 节点的最优值
    - C. 只剪叶子节点
    - D. 剪枝后最佳路径会变化

## 二、简答题（5 分 × 4）

1. **爬山算法**针对**步长**、**局部最优**、**初始点**三个问题的改进算法有哪些？
2. 简述**启发式算法的松弛问题**（relaxed problem）以及**刻画启发式质量**的方法。
3. 简述**决策树学习**适用的问题特点。
4. 简述**贝叶斯学习**的核心思想。

## 三、大题（10 分 × 4）

1. （1）给出 **A\* 启发式算法**的表达式；
   （2）给定树（含每个节点的 $h$ 值与边的实际代价），用**迭代最佳优先搜索**求最优路径。

2. 给定 5 个自然语言句子：
   （1）写出谓词表达；
   （2）化为合取范式；
   （3）用**反证法**证明结论，并写出**归结合一**过程。

3. **机器人 MDP 建模**（与往年一样）。

4. （小测题推广到一般）解释为什么 **EnjoySport** 学习任务的**假设空间大小为 973**；如果增加属性 Water-Current（取值 Light / Moderate / Strong），可能的**实例数**和**假设数**将增加多少？推广到一般：增加两个属性 $A$ 和 $B$，分别有 $k_A$ 和 $k_B$ 种取值，实例数与假设数分别增加多少？
